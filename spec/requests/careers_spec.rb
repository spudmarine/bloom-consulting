require 'spec_helper'

describe "Careers" do
  describe "GET /careers" do
    it "works! (now write some real specs)" do
      get careers_path
      expect(response.status).to be(200)
    end
  end
end
