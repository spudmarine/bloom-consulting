<%= form_for [:admin, @quote], :html => {:multipart => true} do |f| %>
  <% if @quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @quote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :quote %><br>
    <%= f.text_area :quote, :class => "tinymce", :id => "editor", :rows => 10, :cols => 10 %>
  </div>
  <div class="field">
    <%= f.label :credit %><br>
    <%= f.text_area :credit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= tinymce %>
